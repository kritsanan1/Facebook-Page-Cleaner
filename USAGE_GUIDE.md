# คู่มือการใช้งาน Facebook Page Cleaner

## 📋 เตรียมความพร้อมก่อนใช้งาน

### สิ่งที่คุณต้องมี:

1. **บัญชี Facebook** ที่มีสิทธิ์ Admin หรือ Editor ในเพจที่ต้องการจัดการ
2. **Facebook App** (สำหรับรับ Access Token)
3. **เพจ Facebook** ที่ต้องการลบข้อมูล

---

## 🔑 วิธีการรับ Facebook Access Token

### ขั้นตอนที่ 1: สร้าง Facebook App (ถ้ายังไม่มี)

1. ไปที่ [Facebook Developers](https://developers.facebook.com/)
2. คลิก **"My Apps"** → **"Create App"**
3. เลือก **"Business"** type
4. กรอกข้อมูล:
   - **App Display Name**: ตั้งชื่อตามต้องการ (เช่น "My Page Manager")
   - **App Contact Email**: อีเมลติดต่อ
5. คลิก **"Create App"**

### ขั้นตอนที่ 2: เพิ่ม Facebook Login Product

1. ในหน้า App Dashboard คลิก **"Add Product"**
2. เลือก **"Facebook Login"** → คลิก **"Set Up"**
3. เลือก **"Web"**
4. ใส่ Site URL: `https://localhost` (สำหรับ testing)
5. บันทึกและดำเนินการต่อ

### ขั้นตอนที่ 3: สร้าง Access Token

#### วิธีที่ 1: ใช้ Graph API Explorer (แนะนำสำหรับ Testing)

1. ไปที่ [Graph API Explorer](https://developers.facebook.com/tools/explorer/)

2. **เลือก Application:**
   - คลิกที่ dropdown "Facebook App"
   - เลือก App ที่คุณสร้างไว้

3. **เลือก Permissions:**
   - คลิก **"Get User Access Token"**
   - เลือก permissions ต่อไปนี้:
     - ✅ `pages_show_list` - ดูรายชื่อเพจ
     - ✅ `pages_read_engagement` - อ่านข้อมูลโพสต์
     - ✅ `pages_manage_posts` - จัดการโพสต์ (ลบโพสต์)
   - คลิก **"Generate Access Token"**

4. **ยืนยันสิทธิ์:**
   - Facebook จะขออนุญาตเข้าถึงเพจของคุณ
   - คลิก **"Continue as [Your Name]"**
   - เลือกเพจที่ต้องการให้สิทธิ์
   - คลิก **"Done"**

5. **Copy Token:**
   - Token จะปรากฏในช่อง "Access Token"
   - คลิก **"Copy"** หรือคัดลอกด้วยตนเอง

#### วิธีที่ 2: ใช้ Access Token Debugger (สำหรับ Long-lived Token)

Token จาก Graph Explorer จะหมดอายุใน 1-2 ชั่วโมง หากต้องการ Token ที่อายุยาวขึ้น:

1. ไปที่ [Access Token Debugger](https://developers.facebook.com/tools/debug/accesstoken/)
2. วาง Token ที่ได้จาก Graph Explorer
3. คลิก **"Debug"**
4. ดูข้อมูล Token:
   - **Type**: User Access Token
   - **Expires**: เวลาหมดอายุ
   - **Scopes**: permissions ที่มี

5. **ขยายอายุ Token:**
   - คลิก **"Extend Access Token"**
   - Token ใหม่จะอายุ 60 วัน
   - Copy Token ใหม่นี้ไปใช้งาน

---

## 🚀 วิธีใช้งาน Facebook Page Cleaner

### ขั้นตอนที่ 1: เปิดเว็บแอปพลิเคชัน

เปิดเว็บเบราว์เซอร์และไปที่:
```
https://3000-i4ugmz7xnucqs0rt3ua1a-8f57ffe2.sandbox.novita.ai
```

หรือ Production URL (เมื่อ deploy แล้ว)

### ขั้นตอนที่ 2: ใส่ Access Token

1. วาง **User Access Token** ที่คุณได้จาก Graph API Explorer
2. คลิกปุ่ม **"โหลดรายชื่อเพจ"**
3. รอสักครู่ ระบบจะดึงรายชื่อเพจทั้งหมดที่คุณมีสิทธิ์จัดการ

### ขั้นตอนที่ 3: เลือกเพจ

1. คลิกเลือกเพจที่ต้องการจัดการจากรายการ
2. ระบบจะเปลี่ยนสีพื้นหลังเป็นสีฟ้าเมื่อเลือก
3. หน้าต่าง "เลือกการดำเนินการ" จะปรากฏขึ้น

### ขั้นตอนที่ 4: เลือกประเภทการลบ

เลือกหนึ่งในสามตัวเลือก:

#### 🔴 ลบโพสต์ทั้งหมด (Delete All Posts)
- ลบโพสต์ทั้งหมดบนเพจ
- รวมทั้งโพสต์ที่แชร์, สเตตัส, ลิงก์
- **ไม่รวม**: โพสต์ที่ถูกปักหมุด (Pinned posts)

#### 🟣 ลบรูปภาพทั้งหมด (Delete All Photos)
- ลบรูปภาพที่อัปโหลดโดยเพจ
- ลบทั้งรูปในอัลบั้มและรูปที่อัปโหลดแยก
- **ไม่รวม**: รูปโปรไฟล์และรูปปก

#### 🟠 ลบวิดีโอทั้งหมด (Delete All Videos)
- ลบวิดีโอทั้งหมดที่อัปโหลดบนเพจ
- รวมวิดีโอ Live ที่บันทึกไว้
- **ไม่รวม**: วิดีโอที่กำลัง Live อยู่

### ขั้นตอนที่ 5: ยืนยันและรอผลลัพธ์

1. **ยืนยันการลบ:**
   - ป๊อปอัปจะขอยืนยัน: "คุณแน่ใจหรือไม่ว่าต้องการลบ...?"
   - คลิก **"OK"** เพื่อดำเนินการต่อ
   - คลิก **"Cancel"** เพื่อยกเลิก

2. **ติดตามความคืบหน้า:**
   - หน้าจอจะแสดง Progress Bar
   - สถิติแบบ Real-time:
     - **ทั้งหมด**: จำนวนรายการทั้งหมด
     - **สำเร็จ**: จำนวนที่ลบสำเร็จ ✅
     - **ล้มเหลว**: จำนวนที่ลบไม่สำเร็จ ❌
   - Log แสดงรายละเอียดการลบแต่ละรายการ

3. **หยุดการทำงาน (ถ้าต้องการ):**
   - คลิกปุ่ม **"หยุดการทำงาน"**
   - ระบบจะหยุดการลบทันที
   - ข้อมูลที่ลบไปแล้วจะไม่สามารถกู้คืนได้

4. **ผลลัพธ์:**
   - เมื่อเสร็จสิ้น ระบบจะแสดงสรุปผล
   - "เสร็จสิ้น! ลบสำเร็จ: X รายการ, ล้มเหลว: Y รายการ"

---

## ⚠️ คำเตือนและข้อควรระวัง

### ⛔ สิ่งที่ควรทราบก่อนใช้งาน

1. **การลบเป็นการถาวร (Permanent)**
   - ข้อมูลที่ลบไปแล้วไม่สามารถกู้คืนได้
   - Facebook ไม่มีระบบ "ถังขยะ" สำหรับข้อมูลที่ลบ
   - **แนะนำ**: สำรองข้อมูลก่อนใช้งาน

2. **Rate Limiting**
   - Facebook จำกัดจำนวนคำขอต่อนาที
   - ระบบมี delay 100ms ระหว่างคำขอเพื่อป้องกันปัญหานี้
   - หากมีข้อมูลจำนวนมาก อาจใช้เวลานาน

3. **Token Security**
   - **อย่าแชร์ Access Token ให้ผู้อื่น**
   - Token นี้มีสิทธิ์เต็มในการจัดการเพจของคุณ
   - เก็บ Token ไว้เป็นความลับ
   - ระบบไม่เก็บ Token บนเซิร์ฟเวอร์

4. **Token Expiration**
   - Short-lived Token หมดอายุภายใน 1-2 ชั่วโมง
   - Long-lived Token หมดอายุภายใน 60 วัน
   - หาก Token หมดอายุ ต้องสร้างใหม่

5. **Permissions Required**
   - ต้องมีสิทธิ์ **Admin** หรือ **Editor** บนเพจ
   - หาก Token ไม่มี permissions ที่จำเป็น จะลบไม่ได้

---

## 🐛 การแก้ไขปัญหาที่พบบ่อย

### ปัญหา: "ไม่พบเพจที่คุณดูแล"

**สาเหตุ:**
- Token ไม่มี permission `pages_show_list`
- บัญชีของคุณไม่มีเพจที่ดูแล

**วิธีแก้:**
1. ตรวจสอบว่าคุณมีเพจ Facebook หรือไม่
2. สร้าง Token ใหม่พร้อม permission ที่ถูกต้อง
3. ตรวจสอบว่าคุณมีสิทธิ์ Admin/Editor ในเพจ

### ปัญหา: "เกิดข้อผิดพลาด: Invalid OAuth access token"

**สาเหตุ:**
- Token หมดอายุ
- Token ไม่ถูกต้อง
- Token ถูก revoke

**วิธีแก้:**
1. สร้าง Token ใหม่จาก Graph API Explorer
2. ตรวจสอบว่าคัดลอก Token ครบถ้วน
3. ลองใช้ Long-lived Token แทน

### ปัญหา: "ไม่สามารถลบโพสต์: (#200) Insufficient permissions"

**สาเหตุ:**
- ไม่มี permission `pages_manage_posts`
- ไม่มีสิทธิ์เพียงพอบนเพจ

**วิธีแก้:**
1. สร้าง Token ใหม่พร้อม `pages_manage_posts`
2. ตรวจสอบสิทธิ์ของคุณบนเพจ (ต้องเป็น Admin หรือ Editor)

### ปัญหา: "Rate Limit Exceeded"

**สาเหตุ:**
- ทำคำขอมากเกินไปในระยะเวลาสั้น

**วิธีแก้:**
1. รอสักครู่ (5-10 นาที) แล้วลองใหม่
2. ระบบมี delay 100ms อยู่แล้ว แต่อาจไม่เพียงพอสำหรับข้อมูลจำนวนมหาศาล
3. หากยังเกิดปัญหา ให้หยุดชั่วคราวแล้วดำเนินการต่อทีหลัง

---

## 💡 เทคนิคการใช้งานที่มีประสิทธิภาพ

### 1. สำรองข้อมูลก่อนลบ

ใช้เครื่องมือสำรองข้อมูล Facebook:
- [Facebook Business Suite](https://business.facebook.com/) → ตั้งค่า → ดาวน์โหลดข้อมูล
- หรือใช้ [Facebook Data Export](https://www.facebook.com/help/212802592074644)

### 2. ทดสอบกับเพจทดลองก่อน

- สร้างเพจทดสอบ
- ลองใช้งานเครื่องมือกับเพจทดสอบก่อน
- เมื่อมั่นใจแล้วค่อยใช้กับเพจจริง

### 3. ลบทีละประเภท

- อย่าลบทุกอย่างพร้อมกันทันที
- ลบโพสต์ก่อน → ตรวจสอบ → ลบรูปภาพ → ตรวจสอบ → ลบวิดีโอ
- วิธีนี้ช่วยให้คุณควบคุมได้ดีขึ้น

### 4. ตรวจสอบ Log ให้ละเอียด

- อ่าน Log ทุกรายการ
- หากมีรายการที่ลบไม่ได้ ให้จดไว้
- อาจต้องลบด้วยมือสำหรับรายการพิเศษ

---

## 📞 ช่วยเหลือเพิ่มเติม

### เอกสารประกอบ

- [Facebook Graph API Documentation](https://developers.facebook.com/docs/graph-api)
- [Page Access Tokens](https://developers.facebook.com/docs/pages/access-tokens)
- [Permissions Reference](https://developers.facebook.com/docs/permissions/reference)

### แหล่งข้อมูลเพิ่มเติม

- [Facebook Developer Community](https://developers.facebook.com/community/)
- [Stack Overflow - Facebook API](https://stackoverflow.com/questions/tagged/facebook-graph-api)

---

## ✅ Checklist ก่อนใช้งาน

- [ ] มีบัญชี Facebook และเป็น Admin/Editor ของเพจ
- [ ] สร้าง Facebook App แล้ว
- [ ] ได้ User Access Token ที่มี permissions ครบ:
  - [ ] `pages_show_list`
  - [ ] `pages_read_engagement`
  - [ ] `pages_manage_posts`
- [ ] สำรองข้อมูลเพจแล้ว (ถ้าจำเป็น)
- [ ] พร้อมที่จะลบข้อมูลอย่างถาวร
- [ ] อ่านคำเตือนและเข้าใจความเสี่ยงแล้ว

---

**สร้างโดย**: Facebook Page Cleaner Team  
**อัพเดทล่าสุด**: 2025-12-16  
**เวอร์ชัน**: 1.0.0
